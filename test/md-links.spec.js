const mdLinks = require("../index.js");
const {
  isDirectory,
  getAbsolutePath,
  pathExist,
  readDir,
  mdFile,
  linksInfo,
  validateLinks,
  linkStats,
  readingFile,
} = require("../functions");

describe("mdLinks", () => {
  it("should...", () => {
    console.log("FIX ME!");
  });
});
